[project]
name = "epoxy_diffusion"
version = "1.0.0"
description = "Epoxy composite design using diffusion models"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "dragon-ml-toolbox[ml]==22.15.4",
]

[dependency-groups]
dev = [
    "jupytext>=1.19.1",
]

# Jupytext configuration
[tool.jupytext]
formats = "ipynb,md"
